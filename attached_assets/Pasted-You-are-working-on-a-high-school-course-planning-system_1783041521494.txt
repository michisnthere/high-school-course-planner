You are working on a high school course planning system with a fully migrated Prisma schema (PostgreSQL).

Your task is to fix and finalize the data import + normalization pipeline ONLY so it correctly matches the new schema.

DO NOT modify:

Prisma schema
frontend
UI logic
database migrations

Only modify backend data processing.

1. PRIMARY GOAL

Make the import pipeline fully:

idempotent
schema-compliant
consistent with normalization rules
safe to re-run without duplication
2. NORMALIZATION RULES (CRITICAL)

Normalize course catalog hierarchy as:

Department → Subdepartment → Course

Rules:

Split compound departments like:
"Applied Arts–Business Education"
→ department: "Applied Arts"
→ subdepartment: "Business Education"
Do NOT create fake subdepartments like "General"
If no subdepartment exists:
subdepartment = null
Department names must be standardized into:
Applied Arts, Fine Arts, Language Learning, Mathematics, Science, Social Studies, Physical Education, Communication Arts, Computer Science / Engineering / Technology
3. COURSE STRUCTURE OUTPUT

Each Course must include:

title
description
department (normalized)
department_raw
subdepartment (nullable)
gpaWaiverOption
isOnline
notes
importKey = sourceReference + "::" + title
offerings[]
4. COURSE OFFERING RULES

Each CourseOffering must include:

courseCode (UNIQUE KEY for upsert)
semester (INT ONLY)
durationUnits (supports 1.0, 1.5, 2.0)
credits
gradeLevels
creditType
prerequisites

IMPORTANT:

courseCode is the ONLY upsert key for CourseOffering
NEVER merge offerings across different courseCodes
5. IMPORT SCRIPT RULES

In import_courses.ts:

Course upsert uses ONLY importKey
CourseOffering upsert uses ONLY courseCode
Must be fully idempotent
Re-running script must not duplicate or corrupt data
Do NOT silently merge conflicting courses
Log all conflicts into failedRecords
6. GRADUATION REQUIREMENTS
Graduation requirements are static entities
They are linked to Course via RequirementCourseMap
Do NOT compute requirements during import
Do NOT infer eligibility dynamically in import
7. VALIDATION REQUIREMENTS

Before writing to DB:

ensure no duplicate courseCodes
ensure no duplicate importKeys
ensure all required fields exist
ensure offerings are valid

If conflict exists:

log it
skip record safely
do NOT overwrite silently
8. OUTPUT EXPECTATION

Return:

updated normalization code
updated import script changes
any fixes required for schema alignment (if any)
summary of changes made