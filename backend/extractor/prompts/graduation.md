# Graduation requirements prompt

You will be given a set of page images that contain graduation requirements. Produce a JSON object with the following keys: `departments` (empty list), `courses` (empty list), `graduationRequirements` (structured list), `warnings`.

Return only valid JSON.
